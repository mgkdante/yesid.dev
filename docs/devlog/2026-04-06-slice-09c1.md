# Dev Log — 2026-04-06

## Slice: 09c-1 — Foundation: DRY Extraction + Quick Wins

### Session Start
- **Time:** ~09:30
- **Slice spec:** docs/slices/slice-09c-polish.md (split into 09c-1 + 09c-2)
- **Goal:** Extract duplicated patterns, standardize hovers, apply motion actions, fix i18n

### Work Done
- [x] CollapsibleSection.svelte — reusable collapsible card component
  - Files: `src/lib/components/CollapsibleSection.svelte`, `.test.ts`
  - Decision: `$bindable()` open prop for parent sync (ToC use case)
  - Decision: Svelte 5 snippets for icon + children slots

- [x] Refactored Work/Service/Blog detail pages to use CollapsibleSection
  - Files: `WorkDetailPage`, `ServiceDetailPage`, `BlogContent`
  - Decision: `use:reveal` wrappers on native `<div>` since actions need native elements
  - Decision: BlogContent uses `collapsible={false}` with `contentTitle` prop

- [x] FilterGroup.svelte — reusable filter button group
  - Files: `src/lib/components/FilterGroup.svelte`, `.test.ts`
  - Decision: `use:ripple` built into FilterGroup (QW-3 bundled here)

- [x] Refactored Blog/Work filter sidebars to use FilterGroup
  - Files: `BlogFilterSidebar`, `WorkFilterSidebar`

- [x] Card hover standardization (300ms ease, glow, box-shadow)
  - Files: `BlogRow`, `WorkCard`, `ProjectMiniCard`

- [x] use:tilt on WorkCard, use:magnetic on tag pills
  - Files: `WorkCard`, `BlogRow`
  - Learning: tilt must go on inner element, NOT the FLIP target `<a>` — tilt's inline transition conflicts with GSAP Flip

- [x] Reading time badge + localized back links
  - Files: `BlogDetailHeader`, `blog/[slug]/+page.ts`

- [x] Clickable tech tags in WorkDetailSidebar
  - Files: `WorkDetailSidebar`

- [x] Hardcoded content audit — localized all user-facing strings
  - Files: `FilterGroup`, `WorkCard`, `WorkDetailSidebar`, `WorkDetailPage`, `ServiceDetailPage`

- [x] Fixed FLIP animation bug
  - Files: `WorkListingPage`, `WorkCard`
  - Decision: Removed `absolute:true` from FLIP config
  - Decision: Kill competing reveal tweens + reset opacity before FLIP runs
  - Learning: use:reveal re-initializes on Svelte re-render, sets opacity:0, which FLIP then animates toward

- [x] Fixed ToC not syncing with README collapse
  - Files: `CollapsibleSection`, `WorkDetailPage`
  - Decision: Made `open` a `$bindable()` prop

### Blockers / Questions
- None — all resolved during implementation

### Session End
- **Files created:** CollapsibleSection.svelte, CollapsibleSection.test.ts, FilterGroup.svelte, FilterGroup.test.ts
- **Files modified:** WorkDetailPage, ServiceDetailPage, BlogContent, BlogFilterSidebar, WorkFilterSidebar, WorkCard, BlogRow, ProjectMiniCard, BlogDetailHeader, WorkDetailSidebar, WorkListingPage, blog/[slug]/+page.ts, blog/[slug]/+page.svelte
- **Tests passing:** 314/314
- **Ready for handoff:** yes
