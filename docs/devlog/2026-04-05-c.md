# Dev Log — 2026-04-05 (Session C)

## Slice: B+ — Geometric Shape Morphs → Icon Morphs

### Session Start
- **Time:** ~12:00
- **Slice spec:** docs/slices/slice-b-plus-shape-morphs.md
- **Goal:** Iterate on shape morphs based on Yesid feedback, finalize and ship

### Work Done
- [x] Reposition keyword shapes from panel edge to after text content
  - Files changed: `src/lib/components/SkillsJourney.svelte`
  - Multiple positioning iterations (above, left, right, panel edge, after text)

- [x] **Pivot: Remove keyword shapes, morph skill icons instead**
  - Files changed: `SkillsJourney.svelte`, `gsap.ts`, `gsap.test.ts`, `setup.ts`
  - Decision: Yesid said keyword shapes were "too much stuff" — icon morphs are cleaner
  - Learning note: MorphSVGPlugin.convertToPath() converts <rect>/<circle> to <path> in-place

- [x] Panel 1 animations fire on vertical scroll down
  - Decision: Panel 1 is already visible when section pins — horizontal containerAnimation triggers don't fire
  - Solution: Use regular ScrollTrigger with `trigger: pinContainer` and vertical start/end values

- [x] Delay panel 1 animation start (triggers shifted later)
  - `top 35%` → `top -5%` for text, similar for icons

- [x] Replace hold-zone timeline with ScrollTrigger snap
  - Decision: Empty tweens felt "throttled af" per Yesid
  - `snap: { snapTo: [0, 0.25, 0.5, 0.75, 1], duration: { min: 0.2, max: 0.6 } }`

- [x] "motion" word rotates from center
  - `gsap.set(hw, { transformOrigin: 'center center' })`

- [x] "motion" animation starts earlier
  - Trigger widened: `left 55%` → `left 75%`

- [x] Mobile scroll multiplier (1.8× vs 1.4× desktop)
  - Decision: Touch swipes cover more scroll distance per gesture

### Blockers / Questions
- None — all resolved through iteration

### Session End
- **Files created:** `docs/handoffs/handoff-slice-b-plus.md`, `docs/devlog/2026-04-05-c.md`
- **Files modified:** `src/lib/components/SkillsJourney.svelte`, `src/lib/motion/utils/gsap.ts`, `src/lib/motion/utils/gsap.test.ts`, `src/tests/setup.ts`
- **Tests passing:** yes (272/272)
- **Ready for handoff:** yes — Yesid approved
