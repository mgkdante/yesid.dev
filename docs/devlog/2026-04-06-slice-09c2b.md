# Dev Log — 2026-04-06

## Slice: 09c-2b — Polish Enhancements (Final Iteration Fixes)

### Session Start
- **Time:** 18:00
- **Slice spec:** `docs/slices/slice-09c2-polish-enhancements.md`
- **Goal:** Fix two remaining bugs from 09c-2b iteration: invisible metro lines on /work, and hover interrupting SVG entrance animations. Plus a layout tweak (remove top padding on /work).

### Work Done

- [x] Task 1: Fix metro lines on /work page
  - **Files modified:** `src/lib/components/WorkListingPage.svelte`
  - **Decision:** Replaced SVG `url(#metro-grad-{i})` gradient with direct `stroke="#E07800"` — gradient imperceptible on 2px line and `url(#id)` broken in SPA context
  - **Learning note:** SVG paint-server `url(#id)` references resolve relative to the document base URI, which changes during SPA navigation. Direct colors or CSS gradients avoid this issue.

- [x] Task 2: Add hover lock during entrance animations
  - **Files modified:** `src/lib/components/BlogSvgIcon.svelte`, `src/lib/components/WorkSvgIcon.svelte`
  - **Decision:** Used `entranceDone` boolean flag set by GSAP timeline `onComplete`. Refactored BlogSvgIcon's 4 animation functions from raw `gsap.to()` to `gsap.timeline()` wrappers so `onComplete` fires once per animation, not per-element.

- [x] Task 3: Remove top padding on /work
  - **Files modified:** `src/routes/work/+page.svelte`
  - **Decision:** Dropped `pt-12` class from `<main>` wrapper

### Commands Executed

```bash
bun run dev
bunx vitest run --reporter=verbose
bun run check
```

### Validation Results

| Command | Result | Notes |
|---------|--------|-------|
| `bun run test` | PASS | 329 tests, 42 files, 0 failures |
| `bun run check` | PASS | 0 errors, 10 pre-existing warnings |

### Packages Added

| Package | Why |
|---------|-----|
| (none) | |

### Errors Encountered

| Error | Cause | Fix | Resolved |
|-------|-------|-----|----------|
| (none) | | | |

### Blockers / Questions
- None

### Session End (Session 1)
- **Time:** 19:00
- **Files modified:** `WorkListingPage.svelte`, `BlogSvgIcon.svelte`, `WorkSvgIcon.svelte`, `+page.svelte`
- **Tests passing:** yes

---

## Session 2 — 2026-04-07

### Session Start
- **Time:** 07:00
- **Goal:** Fix clear filters, add tech stack filter, match padding, scrollable filters, collapsible filter sections.

### Work Done

- [x] Task 4: Fix "clear filters" not resetting card visibility
  - **Files modified:** `src/lib/components/WorkListingPage.svelte`
  - **Decision:** Added `batchFired` + `$effect` pattern (same as BlogListingPage) to reset opacity after filter changes. Routed clear button through `updateFilter()` instead of raw `goto()`.

- [x] Task 5: Add tech stack filter to /work
  - **Files created:** None (new function in existing file)
  - **Files modified:** `src/lib/data/projects.ts`, `src/lib/data/index.ts`, `src/routes/work/+page.ts`, `src/routes/work/+page.svelte`, `WorkListingPage.svelte`, `WorkFilterSidebar.svelte`, `WorkFilterMobile.svelte`
  - **Decision:** Third filter dimension with AND logic via `stack` URL param. `getAllStackItems()` follows same pattern as `getAllTags()`.

- [x] Task 6: Match work listing padding to blog
  - **Files modified:** `src/routes/work/+page.svelte`
  - **Decision:** Removed redundant `<main class="mx-auto max-w-5xl px-4 md:px-8">` wrapper. Root layout already provides `mx-auto w-full max-w-5xl px-6 pt-20`.

- [x] Task 7: Scrollable filter sections
  - **Files modified:** `WorkFilterSidebar.svelte`, `BlogFilterSidebar.svelte`, `WorkFilterMobile.svelte`, `BlogFilterMobile.svelte`
  - **Decision:** Desktop: `max-h-[calc(100vh-6rem)] overflow-y-auto`. Mobile: `max-h-[60vh] overflow-y-auto`. Scroll only appears when content exceeds bounds.

- [x] Task 8: Collapsible filter sections
  - **Files modified:** `src/lib/components/FilterGroup.svelte`, `WorkFilterSidebar.svelte`, `BlogFilterSidebar.svelte`
  - **Decision:** Added `collapsible` and `startOpen` props to FilterGroup. Services starts open, Tech Stack and Tags start collapsed. Blog date range section made collapsible inline (not a FilterGroup).

### Validation Results

| Command | Result | Notes |
|---------|--------|-------|
| `bun run test` | PASS | 201 data+component tests pass |
| `bun run check` | PASS | 0 errors |

### Packages Added

| Package | Why |
|---------|-----|
| (none) | |

### Errors Encountered

| Error | Cause | Fix | Resolved |
|-------|-------|-----|----------|
| (none) | | | |

### Session End
- **Time:** 08:40
- **Files created:** (docs only)
- **Files modified:** `WorkListingPage.svelte`, `projects.ts`, `index.ts`, `+page.ts`, `+page.svelte`, `FilterGroup.svelte`, `WorkFilterSidebar.svelte`, `WorkFilterMobile.svelte`, `BlogFilterSidebar.svelte`, `BlogFilterMobile.svelte`, `CLAUDE.md`
- **Tests passing:** yes
- **Ready for handoff:** yes
