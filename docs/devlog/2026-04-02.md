# Dev Log — 2026-04-02

## Slice: 02 — Data Layer

### Session Start
- **Time:** 11:00
- **Slice spec:** docs/slices/slice-02-data-layer.md
- **Goal:** Create the typed, i18n-ready data layer that powers every page on the site.

### Work Done

- [x] Create `src/lib/data/types.ts`
  - Files changed: `src/lib/data/types.ts`
  - Decision: Used `interface` for extendable object shapes (Project, Service, SiteMeta, SiteLinks, ProjectSection) and `type` for unions (Locale, ProjectStatus). Interfaces allow declaration merging if needed later; types are cleaner for unions.
  - Decision: `icon` on Service is `string | undefined`, not an enum. Slice 03 picks the icon library — the data layer should not couple to it.
  - Learning note: `LocalizedString` makes i18n a data concern, not a UI concern. Components never branch on locale themselves — they call `resolveLocale()` and get a plain string back.

- [x] Create `src/lib/data/locale.ts`
  - Files changed: `src/lib/data/locale.ts`
  - Decision: Direct fallback to `en` only — no intermediate chain. `resolveLocale({ en: "Hello", fr: "Bonjour" }, 'es')` returns `"Hello"`, not `"Bonjour"`. Simple, predictable, matches the COALESCE analogy in the spec.
  - Decision: Empty strings are treated as missing values. An empty `fr: ""` falls through to English. This prevents half-filled translations from surfacing as blank UI.

- [x] Create `src/lib/data/locale.test.ts`
  - Files changed: `src/lib/data/locale.test.ts`
  - 10 tests covering all locale permutations, empty string edge case, whitespace edge case, and constant validation.

- [x] Create `src/lib/data/projects.ts`
  - Files changed: `src/lib/data/projects.ts`
  - Decision: Helper functions co-located with data. They operate on nothing outside this file — splitting them would be artificial.
  - Seed projects: `yesid-dev` (public, featured) and `transit-data-pipeline` (private, not featured). Two projects exercise all filter paths in the helpers.
  - Decision: Used `readonly Project[]` type annotation instead of `as const`. `as const` would narrow every string to its literal type, making the array incompatible with the `Project` interface that expects `string`.

- [x] Create `src/lib/data/services.ts`
  - Files changed: `src/lib/data/services.ts`
  - 4 services matching the spec. English-only content; icon field uses string identifiers so slice 03 can bind them to any icon library without touching data files.

- [x] Create `src/lib/data/meta.ts`
  - Files changed: `src/lib/data/meta.ts`
  - All four links from spec included (email, github, linkedin, upwork). SEO description kept under 160 characters.

- [x] Create `src/lib/data/index.ts`
  - Files changed: `src/lib/data/index.ts`
  - Barrel re-exports everything. Downstream slices import from `$lib/data` rather than from individual files — stable import path even if internal structure changes.

- [x] Create `src/lib/data/projects.test.ts`
  - Files changed: `src/lib/data/projects.test.ts`
  - 14 tests covering all 4 helpers: valid/invalid slug, featured filter, public filter, tag deduplication and sorting.

- [x] Create `src/lib/data/data-integrity.test.ts`
  - Files changed: `src/lib/data/data-integrity.test.ts`
  - 13 tests validating slug uniqueness, URL-safety, empty field detection, status enum, services count, brand name enforcement.
  - These tests act as a guardrail: any future data entry error (typo, duplicate slug, empty translation) fails loudly at test time, not at runtime.

### Blockers / Questions
- None. All acceptance criteria met.

### Session End
- **Files created:** `src/lib/data/types.ts`, `src/lib/data/locale.ts`, `src/lib/data/locale.test.ts`, `src/lib/data/projects.ts`, `src/lib/data/projects.test.ts`, `src/lib/data/services.ts`, `src/lib/data/meta.ts`, `src/lib/data/data-integrity.test.ts`, `src/lib/data/index.ts`, `docs/devlog/2026-04-02.md`, `docs/handoffs/handoff-slice-02.md`
- **Files modified:** `docs/reference/ARCHITECTURE.md`, `tree.txt`
- **Tests passing:** yes — 41 tests across 4 files (38 new + 3 from slice 01)
- **Ready for handoff:** yes

---

## Slice: 03 — Component Library

### Session Start
- **Time:** 14:20
- **Slice spec:** docs/slices/slice-03-components.md
- **Goal:** Build the 6 reusable UI components that render data from the data layer, each with full test coverage.

### Work Done

- [x] Create `src/lib/components/TagList.svelte` + `TagList.test.ts`
  - Files changed: `src/lib/components/TagList.svelte`, `src/lib/components/TagList.test.ts`
  - Decision: Renders nothing (no element in DOM) when tags array is empty. A dangling empty `<ul>` would be invisible but still pollute the DOM. Conditional rendering is cleaner.
  - Decision: Used `rounded-full` for pill shape — more tag-like than `rounded-brand`. Used `font-mono` to tie into the developer/data brand voice.
  - Learning note: `role="list"` + `role="listitem"` are the semantic pair. Screen readers announce "3 items" when tags list has 3 pills. This is the same principle as using the right SQL type — correctness has value beyond what you can see.

- [x] Create `src/lib/components/SectionHeader.svelte` + `SectionHeader.test.ts`
  - Files changed: `src/lib/components/SectionHeader.svelte`, `src/lib/components/SectionHeader.test.ts`
  - Decision: Used `<header>` as the wrapping element (semantic landmark) and `<h2>` for section headings. The home page uses `<h1>` (Hero), sections use `<h2>`, cards use `<h3>` — this keeps the heading hierarchy consistent across slices.
  - Decision: Subtitle rendered as `<p>`, not `<span>` — it is a block-level description, not inline text.

- [x] Create `src/lib/components/ServiceCard.svelte` + `ServiceCard.test.ts`
  - Files changed: `src/lib/components/ServiceCard.svelte`, `src/lib/components/ServiceCard.test.ts`
  - Decision: Icon renders the string identifier as text inside a styled box. `aria-hidden="true"` prevents screen readers from announcing "database" as text content — it's decorative until a real SVG replaces it.
  - Decision: `<article>` wraps each card. An article is self-contained and can stand alone — exactly what a service card is.

- [x] Create `src/lib/components/ProjectCard.svelte` + `ProjectCard.test.ts`
  - Files changed: `src/lib/components/ProjectCard.svelte`, `src/lib/components/ProjectCard.test.ts`
  - Decision: Entire card is wrapped in `<a>` using a `group` class. The inner article gets `group-hover:border-brand-primary`. This makes the whole card clickable without requiring JS click handlers, and Tailwind `group` propagates the hover to the border — a CSS-only interaction.
  - Decision: WIP badge uses `#FFB627` (brand accent = semantic warning). Private badge uses muted surface colors. `public` gets no badge — the absence of a badge communicates "normal/visible" without adding noise.
  - Decision: TagList is conditionally rendered (`{#if tags.length > 0}`) with a `mt-4` wrapper div. This avoids a gap at the card bottom when tags is empty.

- [x] Create `src/lib/components/ProjectGrid.svelte` + `ProjectGrid.test.ts`
  - Files changed: `src/lib/components/ProjectGrid.svelte`, `src/lib/components/ProjectGrid.test.ts`
  - Decision: Keyed `{#each projects as project (project.slug)}`. Using slug as the key gives Svelte a stable identity for each DOM node — avoids full re-renders when the array changes. Slugs are guaranteed unique by data-integrity tests.
  - Decision: Grid renders nothing when array is empty (same pattern as TagList). Callers control what to show in the empty state (e.g., a "no projects yet" message).

- [x] Create `src/lib/components/Hero.svelte` + `Hero.test.ts`
  - Files changed: `src/lib/components/Hero.svelte`, `src/lib/components/Hero.test.ts`
  - Decision: Left-aligned layout. Developer/data portfolios read as more credible when left-aligned — centered hero text reads as a marketing landing page.
  - Decision: CTA row only renders when at least one CTA is provided. No empty flex container in the DOM when both are undefined.
  - Decision: Primary CTA uses `bg-brand-primary` (solid orange fill). Secondary uses a ghost/outline style. This gives visual hierarchy to the two actions — one dominant, one recessive.

### Blockers / Questions
- None. All acceptance criteria met.

### Session End
- **Files created:** `src/lib/components/TagList.svelte`, `src/lib/components/TagList.test.ts`, `src/lib/components/SectionHeader.svelte`, `src/lib/components/SectionHeader.test.ts`, `src/lib/components/ServiceCard.svelte`, `src/lib/components/ServiceCard.test.ts`, `src/lib/components/ProjectCard.svelte`, `src/lib/components/ProjectCard.test.ts`, `src/lib/components/ProjectGrid.svelte`, `src/lib/components/ProjectGrid.test.ts`, `src/lib/components/Hero.svelte`, `src/lib/components/Hero.test.ts`, `docs/handoffs/handoff-slice-03.md`
- **Files modified:** `docs/reference/ARCHITECTURE.md`, `tree.txt`, `docs/devlog/2026-04-02.md`
- **Tests passing:** yes — 75 tests across 10 files (34 new component tests + 41 from slices 01-02)
- **Ready for handoff:** yes


---

## Slice: 04 — Motion Infrastructure + Component Enhancement

### Session Start
- **Time:** 15:30
- **Slice spec:** docs/slices/slice-04-motion.md
- **Goal:** Install animation toolkit, build motion primitives (actions/stores/utils/LottiePlayer), update Service interface for data-driven train journey, wire motion into ProjectCard and TagList.

### Work Done
- [x] Installed dependencies: gsap 3.14.2, @threlte/core 8.5.5, @threlte/extras 9.14.2, three 0.183.2, lottie-web 5.13.0, @types/three 0.183.1
- [x] Created `src/lib/motion/` directory structure per MOTION.md section 12
- [x] Added global test stubs to `src/tests/setup.ts` (matchMedia, IntersectionObserver, GSAP mocks, lottie-web mock)
  - Decision: `vi` must be explicitly imported in setup.ts — `globals: true` applies to test files only, not setup files.
- [x] Built `prefersReducedMotion` store + `isPrefersReducedMotion()` sync helper (SSR-safe, reactive)
- [x] Built `scrollProgress` store (0–1, rAF-gated, SSR-safe, returns 0 when no scrollbar)
- [x] Built GSAP registration utility (idempotent `registerGsapPlugins()`)
- [x] Built stagger utility (`stagger(index, baseDelay)`, ±15% randomization per MOTION.md section 2)
- [x] Built `use:boop` — CSS transform + timeout, no GSAP (simpler, sufficient for a one-shot hover)
- [x] Built `use:reveal` — GSAP ScrollTrigger entrance at 80% viewport, delay in ms converted to seconds internally
- [x] Built `use:magnetic` — used `navigator.maxTouchPoints` not `ontouchstart` (jsdom sets ontouchstart which caused false positives)
- [x] Built `use:ripple` — individual style property assignment instead of cssText (jsdom re-serializes cssText unreliably)
- [x] Built `LottiePlayer.svelte` — dynamic import inside `$effect()` for SSR safety; `goToAndStop(0)` on reduced-motion (static frame, not empty box)
- [x] Updated Service interface: added `id`, `station` (uncapped sequential), `relatedProjects`
- [x] Updated services.ts: all 4 services have new fields; icon points to actual Lottie filenames
- [x] Updated data-integrity.test.ts: removed hardcoded "exactly 4 services", added 8 structural tests (unique ids, sequential stations, valid slugs) — adding a 5th service passes all tests
- [x] Enhanced ProjectCard: `use:boop={{ scale: 1.05, timing: 300 }}`
- [x] Enhanced TagList: `use:reveal` + stagger on each `<li>`
- [x] All 149 tests pass. `bun run check` passes (0 errors, 0 warnings).

### Session End
- **Files created:** `src/lib/motion/` (entire directory, 24 new files), `docs/handoffs/handoff-slice-04.md`
- **Files modified:** `src/lib/data/types.ts`, `src/lib/data/services.ts`, `src/lib/data/data-integrity.test.ts`, `src/lib/components/ProjectCard.svelte`, `src/lib/components/TagList.svelte`, `src/tests/setup.ts`, `docs/reference/ARCHITECTURE.md`, `tree.txt`, `package.json`, `bun.lock`
- **Tests passing:** yes — 149 tests across 19 files (74 new + 75 carried from slices 01–03)
- **Ready for handoff:** yes

---

## Slice: 05 — Layout Shell + Scroll Progress Rail

### Session Start
- **Time:** 20:10
- **Slice spec:** docs/slices/slice-05-layout.md
- **Goal:** Build the root layout shell (Nav, Footer, ScrollRail) that wraps every page, with page transitions and scroll-linked UI.

### Work Done

- [x] Create `src/lib/components/Nav.svelte` + `Nav.test.ts`
  - Files changed: `src/lib/components/Nav.svelte`, `src/lib/components/Nav.test.ts`
  - Decision: Accept `pathname` as a prop instead of reading `$page` directly. The layout passes `$page.url.pathname` down. This keeps Nav testable without mocking SvelteKit stores — same principle as dependency injection in backend code.
  - Decision: Active detection uses `pathname.startsWith(link.href)` so nested routes (e.g., `/work/some-project`) still highlight the parent link. Root `/` is exact-matched to prevent false positives.
  - Decision: Mobile menu uses `$state()` toggle with `{#if menuOpen}` conditional rendering. Hamburger button uses three `<span>` lines that animate into an X via Tailwind transforms.
  - Decision: `aria-current="page"` on active links for accessibility. Screen readers announce "current page" — better than a visual-only highlight.
  - Learning note: `aria-current="page"` is the semantic way to mark active navigation. It's the HTML equivalent of highlighting the current row in a query result.

- [x] Create `src/lib/components/Footer.svelte` + `Footer.test.ts`
  - Files changed: `src/lib/components/Footer.svelte`, `src/lib/components/Footer.test.ts`
  - Decision: Imports `siteMeta` directly from `$lib/data` — it's static data, no store mocking needed in tests.
  - Decision: External links get `target="_blank"` + `rel="noopener noreferrer"` for security. `aria-label` on each link for screen reader clarity.
  - Decision: Minimal layout — brand name with orange period, year, GitHub + LinkedIn. Footer is a signature, not a second nav.

- [x] Create `src/lib/motion/components/ScrollRail.svelte` + `ScrollRail.test.ts`
  - Files changed: `src/lib/motion/components/ScrollRail.svelte`, `src/lib/motion/components/ScrollRail.test.ts`
  - Decision: Two modes driven by `pathname` prop. Home (`/`) shows station dots from `services.length`; all other pages show a simple progress bar. No hardcoded station count — adding a 5th service automatically adds a 5th dot.
  - Decision: Station dot threshold = `index / (services.length - 1)`. Evenly distributed across the scroll range. First dot activates at scroll 0%, last at 100%.
  - Decision: `aria-hidden="true"` on the rail — it's decorative and provides no information that isn't already in the page content.
  - Decision: Respects `$prefersReducedMotion` — transitions on dot color changes and progress bar height skip when reduced motion is on.
  - Learning note: The ScrollRail is data-driven just like the rest of the site. The services array is the single source of truth — the UI derives from data, never the other way around.

- [x] Update `src/routes/+layout.svelte`
  - Files changed: `src/routes/+layout.svelte`
  - Decision: Compose Nav → ScrollRail → `{#key}` main → Footer. The `{#key $page.url.pathname}` block triggers a 250ms CSS fade-in on route change. No exit animation — Svelte 5 `{#key}` unmounts instantly, and a simple fade-in is sufficient.
  - Decision: `pt-20` on main clears the fixed-position Nav (~80px).
  - Decision: Page transition animation skipped when `$prefersReducedMotion` is true (class not applied).

- [x] Simplify `src/routes/+page.svelte`
  - Files changed: `src/routes/+page.svelte`
  - Decision: Removed outer wrapper div — background, text color, and font are now provided by the layout. Kept `data-testid="app-root"` so existing `home.test.ts` passes unchanged.

### Blockers / Questions
- None. All acceptance criteria met.

### Session End
- **Files created:** `src/lib/components/Nav.svelte`, `src/lib/components/Nav.test.ts`, `src/lib/components/Footer.svelte`, `src/lib/components/Footer.test.ts`, `src/lib/motion/components/ScrollRail.svelte`, `src/lib/motion/components/ScrollRail.test.ts`, `docs/handoffs/handoff-slice-05.md`
- **Files modified:** `src/routes/+layout.svelte`, `src/routes/+page.svelte`, `docs/reference/ARCHITECTURE.md`, `docs/devlog/2026-04-02.md`, `tree.txt`
- **Tests passing:** yes — 169 tests across 22 files (20 new + 149 from slices 01-04)
- **Ready for handoff:** yes

---

## Pre-Slice-06: Threlte 3D Scene Preview

### Session Start
- **Time:** 21:00
- **Slice spec:** N/A — pre-slice validation task
- **Goal:** Build and visually verify the Threlte 3D hero background scene before slice 06 wires it to scroll.

### Work Done

- [x] Install `postprocessing@6.39.0` — required for bloom effects via Threlte v8 pattern
  - Files changed: `package.json`, `bun.lock`

- [x] Create `src/lib/motion/three/scene-config.ts`
  - Files changed: `src/lib/motion/three/scene-config.ts`
  - Decision: `getSceneConfig(stationCount)` returns all positions — pure function, no hardcoded counts. N+2 nodes (departure + stations + destination). Primary path uses sine arc for Y and sine*2 for Z variation.
  - Decision: Secondary paths are hand-crafted with 5 control points each, crossing near center at Z=-2.5 and Z=-3.5.
  - Decision: Camera at (0, 0, 5) with FOV 50 — keeps all station nodes visible within the viewport.

- [x] Create `src/lib/motion/three/PostProcessing.svelte`
  - Files changed: `src/lib/motion/three/PostProcessing.svelte`
  - Decision: Followed Threlte v8 manual postprocessing pattern — direct use of `postprocessing` library, not Threlte wrappers. Disables autoRender, manages EffectComposer, renders via `useTask`.
  - Decision: BloomEffect at half resolution (256x256) per MOTION.md S6 constraint. Skip bloom entirely when reducedMotion is on.
  - Decision: Also sets `scene.background = #141414` here since it already has `useThrelte()` access.

- [x] Create `src/lib/motion/three/DataPaths.svelte`
  - Files changed: `src/lib/motion/three/DataPaths.svelte`
  - Decision: Pre-build TubeGeometry once from curves (static). Only material properties animate per-frame via `useTask`.
  - Decision: Primary path emissiveIntensity lerps 0.3→1.0 with `MathUtils.lerp(current, target, delta*3)`. Secondary paths pulse with sine wave at frequencies 1.3 and 0.9 (different to avoid sync).

- [x] Create `src/lib/motion/three/StationNodes.svelte`
  - Files changed: `src/lib/motion/three/StationNodes.svelte`
  - Decision: Shared IcosahedronGeometry, separate materials per node (emissiveIntensity needs to differ). Lerp rate `delta*4` for snappy but smooth transitions.

- [x] Create `src/lib/motion/three/HeroScene.svelte`
  - Files changed: `src/lib/motion/three/HeroScene.svelte`
  - Decision: `activeStation` derived inside HeroScene from `scrollProgress * stationCount`. Camera looks at (0, 0, -1) to frame the primary path arc.

- [x] Create `src/routes/preview/+page.ts` and `+page.svelte`
  - Files changed: `src/routes/preview/+page.ts`, `src/routes/preview/+page.svelte`
  - Decision: `ssr = false` in +page.ts prevents Three.js SSR crashes. Dynamic import via `onMount` as belt-and-suspenders.
  - Decision: Fixed overlay (`fixed inset-0 z-50`) covers root layout Nav/Footer without modifying existing layout files.

- [x] Deleted `src/lib/motion/three/.gitkeep` — replaced by real component files.

### Verification
- `bun run check`: 0 errors, 4 warnings (benign: Svelte 5 "state_referenced_locally" for static geometry init from props)
- `bun run test`: 177 tests pass, 0 regressions (8 new tests from slice 05 work)
- Browser at /preview:
  - Dark background, orange glowing tubes with bloom ✓
  - 4 station nodes along primary path arc ✓
  - Slider at 0%: dim primary path, all nodes dim ✓
  - Slider at 50%: brighter path, 2/4 nodes lit ✓
  - Slider at 100%: full brightness, all 4 nodes lit ✓
  - Secondary paths pulse independently ✓
  - 0 console errors ✓

### Blockers / Questions
- None.

### Session End
- **Files created:** `src/lib/motion/three/scene-config.ts`, `src/lib/motion/three/PostProcessing.svelte`, `src/lib/motion/three/DataPaths.svelte`, `src/lib/motion/three/StationNodes.svelte`, `src/lib/motion/three/HeroScene.svelte`, `src/routes/preview/+page.ts`, `src/routes/preview/+page.svelte`
- **Files modified:** `package.json`, `bun.lock`
- **Files deleted:** `src/lib/motion/three/.gitkeep`
- **Tests passing:** yes — 177 tests, 0 regressions
- **Ready for handoff:** yes

---

## Slice: 06 — Home Page: Train Journey + Hero 3D Scene

### Session Start
- **Time:** 21:50
- **Slice spec:** docs/slices/slice-06-home-journey.md
- **Goal:** Assemble the home page with 3D background, scroll-linked SVG train, station sections, and CTA.

### Work Done
- [x] Create `src/routes/+page.ts` (SSR disabled)
  - Decision: Same pattern as `/preview` route. Three.js/GSAP need browser APIs.

- [x] Move Lottie files from `src/lib/assets/lottie/` to `static/lottie/`
  - Decision: LottiePlayer uses `path:` which fetches by URL. Files in `src/lib/assets/` are build-time imports. Moving to `static/` makes them URL-serveable.

- [x] Modify `src/routes/+layout.svelte` for conditional container
  - Decision: `$derived(isHome)` instead of `{@const}` — the latter can't be placed directly in a `<div>` in Svelte 5.
  - Learning note: `{@const}` must be inside a block tag ({#if}, {#each}, etc.), not a plain element.

- [x] Update `src/tests/setup.ts`
  - Added canvas mock (HTMLCanvasElement.prototype.getContext) — lottie-web CJS code accesses canvas at import time
  - Added @threlte/core mock — jsdom has no WebGL
  - Added postprocessing mock — EffectComposer needs WebGL
  - Added gsap.timeline mock — TrainJourney creates GSAP timelines

- [x] Create `src/lib/components/ScrollPrompt.svelte` + test
  - SVG chevron with CSS bounce animation, respects prefers-reduced-motion via CSS media query

- [x] Create `src/lib/components/ServiceStation.svelte` + test
  - Composes LottiePlayer, SectionHeader, ProjectCard for each station
  - Used `$derived` for reactive title/description resolution
  - Used type guard `isDefined()` to narrow `getProjectBySlug()` return type

- [x] Create `src/lib/motion/svg/train-path.ts` + test
  - Generates cubic bezier SVG path string from station count and viewport dimensions
  - Train travels in the lower viewport (85% height) with gentle sine wave undulation

- [x] Create `src/lib/motion/svg/TrainJourney.svelte` + test
  - Positions Train.svelte in an absolutely positioned wrapper
  - GSAP MotionPathPlugin moves the wrapper along the computed path
  - Wheel rotation and glow pulse animations
  - Timeline scrubbed by scrollProgress prop in $effect

- [x] Replace `src/routes/+page.svelte` with full home page
  - Three visual layers: fixed 3D background, fixed train layer, scrollable content
  - Dynamic import of HeroScene and TrainJourney (desktop only)
  - GSAP ScrollTrigger drives localProgress state
  - Mobile fallback: CSS gradient, no 3D, no train
  - Hero: "yesid." + "The infrastructure is ready." + tagline + scroll prompt
  - 4 ServiceStation sections from services data
  - CTA: "Let's build something that moves." + View work / Get in touch

- [x] Update `src/routes/home.test.ts`
  - 10 tests: hero heading, copy, tagline, scroll prompt, 4 stations, CTA section, CTA links

### Blockers / Questions
- None

### Session End
- **Files created:** `src/routes/+page.ts`, `src/lib/components/ServiceStation.svelte`, `src/lib/components/ServiceStation.test.ts`, `src/lib/components/ScrollPrompt.svelte`, `src/lib/components/ScrollPrompt.test.ts`, `src/lib/motion/svg/train-path.ts`, `src/lib/motion/svg/train-path.test.ts`, `src/lib/motion/svg/TrainJourney.svelte`, `src/lib/motion/svg/TrainJourney.test.ts`, `docs/slices/slice-06-home-journey.md`, `docs/handoffs/handoff-slice-06.md`
- **Files modified:** `src/routes/+page.svelte`, `src/routes/+layout.svelte`, `src/routes/home.test.ts`, `src/tests/setup.ts`, `docs/reference/ARCHITECTURE.md`, `tree.txt`
- **Files moved:** `src/lib/assets/lottie/*.json` → `static/lottie/`
- **Tests passing:** yes — 207 tests, 0 errors
- **Type check:** 0 errors, 4 warnings (pre-existing Threlte)
- **Ready for handoff:** yes
