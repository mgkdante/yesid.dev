# Dev Log — 2026-04-01

## Slice: 01 — Project Scaffold + Brand + CI Pipeline

### Session Start
- **Slice spec:** docs/slices/slice-01-scaffold.md
- **Goal:** Initialize SvelteKit 2 + Svelte 5 project with Bun, Tailwind CSS, Vitest, Playwright, and GitHub Actions CI.

### Work Done

- [x] Scaffold SvelteKit in temp dir and merge
  - Files changed: `src/`, `static/`, `svelte.config.js`, `vite.config.ts`, `tsconfig.json`, `package.json`, `playwright.config.ts`, `.npmrc`
  - Decision: Used `bunx sv create --add tailwindcss --add vitest --add playwright --add sveltekit-adapter=adapter:vercel` to scaffold with all add-ons in a single pass.
  - Learning note: `--no-install` prevents auto-`bun install` inside the temp dir. Merge is just a `cp -r`.

- [x] Edit `package.json`
  - Files changed: `package.json`
  - Decision: Changed name to `yesid-dev`. Changed `"test"` script to `"vitest run"` (removes the e2e test from the default `bun run test`). Added `"test:watch": "vitest"`.

- [x] Merge `.gitignore`
  - Files changed: `.gitignore`
  - Decision: Replaced `.next/` (leftover from a template) with SvelteKit-specific entries: `/.svelte-kit`, `.vercel`, `bun.lockb`, `vite.config.ts.timestamp-*`.

- [x] Install dependencies
  - Ran `bun install`, then `bun add -d @testing-library/svelte @testing-library/jest-dom jsdom`
  - Decision: `@testing-library/svelte@5.3.1` supports Svelte 5.

- [x] Brand tokens integration
  - Files changed: `src/lib/styles/tokens.css` (new), `src/app.css` (new)
  - Decision: Tailwind v4 (CSS-first config) was installed by `sv create` — NOT v3. The spec anticipated this. Translated `brand/tailwind.brand.js` into `@theme` CSS directives in `src/app.css`. The source file `brand/tailwind.brand.js` is kept as documentation.
  - Learning note: In Tailwind v4, instead of `tailwind.config.js`, you configure your design tokens directly in CSS with `@theme`. `--color-brand-primary: #E07800` generates `text-brand-primary`, `bg-brand-primary`, etc. automatically.

- [x] Configure `src/app.html`
  - Files changed: `src/app.html`
  - Decision: Added `class="dark" data-theme="dark"` to `<html>` (dark-first). Added Google Fonts preconnect links for Inter + JetBrains Mono. Added explicit `<link rel="icon">` as a static fallback.

- [x] Move favicon
  - Files changed: `static/favicon.svg` (new), `src/lib/assets/favicon.svg` (replaced Svelte logo)
  - Decision: Root `favicon.svg` deleted after copying to `static/`. `brand/favicon.svg` kept as the source asset.

- [x] Configure Vitest
  - Files changed: `vite.config.ts`, `src/tests/setup.ts` (new)
  - Decision 1: Used flat Vitest config (`environment: 'jsdom'`) instead of the scaffold's workspace/projects config. Simpler and matches the spec's intent for a frontend-only project.
  - Decision 2: Added `svelteTesting()` Vite plugin from `@testing-library/svelte/vite`. This is the official solution for Svelte 5 + jsdom — without it, Svelte 5 loads `index-server.js` (SSR bundle) instead of `index-client.js` (client bundle), causing `mount() is not available on the server` errors.

- [x] Configure Playwright
  - Files changed: `playwright.config.ts`
  - Decision: Changed `webServer.command` from `npm run` to `bun run`. Set `testDir: 'tests'` and `testMatch: '**/*.spec.ts'` per spec.

- [x] Write branded `+page.svelte`
  - Files changed: `src/routes/+page.svelte`
  - Decision: Replaced scaffold placeholder with branded markup from the spec.

- [x] Update `+layout.svelte`
  - Files changed: `src/routes/+layout.svelte`
  - Decision: Changed `import './layout.css'` to `import '../app.css'`. Deleted `src/routes/layout.css` (superseded by `src/app.css`).

- [x] Write smoke test
  - Files changed: `src/routes/home.test.ts` (new)
  - Decision 1: Named `home.test.ts` not `+page.test.ts`. SvelteKit 2 reserves the `+` prefix for framework files — a file named `+page.test.ts` in `src/routes/` causes a build error ("Files prefixed with + are reserved").
  - Decision 2: Used `getByRole('heading', { level: 1 })` instead of `getByText('yesid')`. The `h1` contains "yesid" + a `<span>` child with "." — exact text matching is fragile against child elements.

- [x] GitHub Actions CI
  - Files changed: `.github/workflows/ci.yml` (new)
  - Decision: Exactly as spec. E2E tests excluded per spec (will be added in slice 08).

- [x] Delete `brand/tailwind_brand.js`
  - Files changed: `brand/tailwind_brand.js` (deleted)
  - Decision: Confirmed byte-for-byte duplicate of `tailwind.brand.js`.

- [x] Delete `.gitkeep` files
  - Files changed: `src/.gitkeep`, `tests/.gitkeep`, `scripts/.gitkeep`, `docs/devlog/.gitkeep`, `docs/handoffs/.gitkeep` (all deleted)
  - Also deleted `src/routes/demo/` and `src/lib/vitest-examples/` (scaffold demo files)

- [x] Pin Node.js runtime in `svelte.config.js`
  - Files changed: `svelte.config.js`
  - Decision: Added `runtime: 'nodejs22.x'` to adapter config. Bun ships Node.js v25.8.2 ABI which the Vercel adapter's auto-detection rejects. Explicit pin avoids the error.

### Blockers / Questions
- None. All issues resolved during session.

### Session End
- **Files created:** `src/app.css`, `src/app.html`, `src/lib/styles/tokens.css`, `src/lib/assets/favicon.svg`, `src/routes/home.test.ts`, `src/tests/setup.ts`, `static/favicon.svg`, `.github/workflows/ci.yml`, `tree.txt`, `docs/devlog/2026-04-01.md`, `docs/handoffs/handoff-slice-01.md`
- **Files modified:** `package.json`, `.gitignore`, `vite.config.ts`, `svelte.config.js`, `playwright.config.ts`, `src/routes/+page.svelte`, `src/routes/+layout.svelte`
- **Files deleted:** `brand/tailwind_brand.js`, `src/routes/layout.css`, `favicon.svg` (root), all `.gitkeep` files, `src/routes/demo/`, `src/lib/vitest-examples/`
- **Tests passing:** Yes — 3/3 smoke tests
- **Build passing:** Yes — `bun run build` succeeds
- **Ready for handoff:** Yes

---

## Slice 01 Follow-up — Verification and Fixes

### Session Start
- **Slice spec:** (follow-up, no new spec)
- **Goal:** Verify E2E tests, confirm devlog exists, fix two errors in CLAUDE.md.

### Work Done

- [x] Write and run E2E smoke tests
  - Files changed: `tests/smoke.spec.ts` (new)
  - Decision: The spec said "page loads, tagline visible". Initial test used `toHaveTitle(/yesid/i)` but the page has no `<title>` set in slice 01. Changed "home page loads" assertion to `getByTestId('app-root')` instead. Title test deferred to slice 05+ when real page content is built.
  - Also needed: `bunx playwright install chromium` to download chromium-1217 (project uses Playwright 1.59.1 which requires a newer chromium than was installed).
  - Result: 3/3 E2E tests passing.

- [x] Confirm devlog exists
  - `docs/devlog/2026-04-01.md` was already written during the slice 01 session above.

- [x] Fix CLAUDE.md — Runtime section
  - Files changed: `CLAUDE.md`
  - Decision: Changed "Use `bun test` for Vitest" to "Use `bun run test` for Vitest". `bun test` invokes Bun's own test runner (not Vitest). `bun run test` runs the npm script which calls `vitest run`.

- [x] Fix CLAUDE.md — Handoff Report Format
  - Files changed: `CLAUDE.md`
  - Decision: Added "Files Modified" section to the handoff template, alongside "What Was Built". Future handoffs should track both new files and modified files. The slice 01 handoff already covers this but the template was missing it.

- [x] Update tree.txt
  - Files changed: `tree.txt`

### Blockers / Questions
- None.

### Session End
- **Files created:** `tests/smoke.spec.ts`
- **Files modified:** `CLAUDE.md`, `docs/devlog/2026-04-01.md`, `tree.txt`
- **Tests passing:** Yes — 3/3 unit, 3/3 E2E
- **Ready for handoff:** Yes
