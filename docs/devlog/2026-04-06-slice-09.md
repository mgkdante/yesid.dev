# Dev Log — 2026-04-06

## Slice: 09 — Services Pages (/services + /services/[id])

### Session Start
- **Time:** 07:00
- **Slice spec:** docs/slices/slice-09-services.md
- **Goal:** Build /services index (full-viewport kinetic scroll) and /services/[id] detail (consultative deep dive)

### Work Done

- [x] Extended Service interface with optional detail fields (subtitle, valueProposition, deliverables, stack, sections)
  - Files changed: `src/lib/data/types.ts`, `src/lib/data/index.ts`
  - Decision: All new fields optional for backward compatibility
  - Learning note: Adding optional fields to existing interfaces is safe — no existing code breaks

- [x] Populated all 6 services with detail data + getAdjacentServices helper
  - Files changed: `src/lib/data/services.ts`, `src/lib/data/index.ts`
  - Decision: Content written consultative/client-facing, not developer-facing
  - Learning note: getAdjacentServices sorts by station number, returns prev/next for navigation

- [x] Built StationTabs component — reusable station tab navigation
  - Files changed: `src/lib/components/StationTabs.svelte`, `StationTabs.test.ts`
  - Decision: Two modes (scroll/navigate) for reuse on index + detail pages
  - Learning note: Short labels via lookup map, not computed — easier to maintain per-service

- [x] Built ProofStrip component — compact bottom proof strip
  - Files changed: `src/lib/components/ProofStrip.svelte`, `ProofStrip.test.ts`
  - Decision: Name + count only, horizontally scrollable, hazard stripe below

- [x] Rebuilt ServiceCard component — full-viewport content block
  - Files changed: `src/lib/components/ServiceCard.svelte`, `ServiceCard.test.ts`
  - Decision: Replaced old placeholder card. SVG in rounded morph box. Single SVG element repositioned via CSS (desktop right, mobile above)

- [x] Built ServiceNav component — prev/next service navigation
  - Files changed: `src/lib/components/ServiceNav.svelte`, `ServiceNav.test.ts`
  - Decision: Same pattern as could be reused for work/blog prev/next

- [x] Built ServiceListingPage — full-viewport scroll layout
  - Files changed: `src/lib/components/ServiceListingPage.svelte`
  - Decision: CSS scroll-snap (not GSAP snap) for reliability. Metro line with auto-computed dots. Footer inside scroll container after last station.

- [x] Built ServiceDetailPage — consultative deep dive
  - Files changed: `src/lib/components/ServiceDetailPage.svelte`
  - Decision: Reuses WorkDetailPage collapsible section pattern exactly. All sections open by default. DataFlowDiagram for stack viz.

- [x] Built ProjectMiniCard — reusable project card for outside /work
  - Files changed: `src/lib/components/ProjectMiniCard.svelte`
  - Decision: Compact card with title, one-liner, stack pills, hover lift. Used in services detail related projects.

- [x] Created routes with load functions and 404 handling
  - Files changed: `src/routes/services/+page.ts`, `+page.svelte`, `src/routes/services/[id]/+page.ts`, `+page.svelte`

- [x] Added "Services" to navbar
  - Files changed: `src/lib/components/Nav.svelte`

- [x] Updated layout for full-width services pages
  - Files changed: `src/routes/+layout.svelte`
  - Decision: Services pages break out of max-w-5xl constraint, footer hidden from layout on /services listing

- [x] Added metro/station styling — hazard stripes
  - Files changed: `src/lib/components/StationTabs.svelte`, `src/lib/components/ProofStrip.svelte`
  - Decision: Yellow/black repeating-linear-gradient stripes above tabs and below proof strip

- [x] Fixed WorkDetailPage ToC sync
  - Files changed: `src/lib/components/WorkDetailPage.svelte`
  - Decision: Added syncOpen={readmeOpen} so desktop ToC collapses with README section

### Iterations
Multiple iterations with Yesid feedback:
1. Fixed full-width layout, removed center constraints
2. Added SVG morph hover (CSS :hover + JS onclick for mobile tap)
3. Fixed StationTabs mobile first panel visibility
4. Added hazard stripes for metro station feel
5. Restored DataFlowDiagram after mistakenly removing it
6. Fixed footer placement (inside scroll container, appears after last station)
7. Made all detail sections open by default
8. Removed dark project band, used centered content with ProjectMiniCard

### Blockers / Questions
- None

### Session End
- **Files created:** 12 new files (6 components + tests, 4 route files, 1 ProjectMiniCard, 1 devlog)
- **Files modified:** 8 existing files
- **Tests passing:** yes (304/304)
- **Type check:** 0 errors
- **Ready for handoff:** yes (pending Yesid approval)
