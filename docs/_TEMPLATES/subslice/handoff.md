# <slice-name>/<subslice-name> — Handoff

> **Sub-slices are the PR boundary.** This handoff IS the PR body. Written per-task as tasks land; finalized at `/workflow-close-slice`.

<!-- FILL IN: replace `<slice-name>` and `<subslice-name>` in the title. -->

## PR body

<!-- FILL IN: the PR description for this sub-slice.

**PR direction depends on the parent slice's structure:**
- If parent slice is decomposed into sub-slices with a dedicated parent branch → PR target is the parent slice branch.
- If parent slice is effectively one-level (this sub-slice IS the whole slice) → PR target is the repo's default branch (main/master).

Typical PR body shape:

**Summary** — one paragraph on what this sub-slice delivers and how it advances the parent slice.

**What changed** — bulleted list of user-visible or reviewer-visible changes.

**Verification** — which canonical commands were run and their green state.

**Follow-ups** — anything intentionally deferred. Link to the parent slice's plan if a follow-up sub-slice is already scheduled.
-->

## Peer review notes

<!-- FILL IN: the other AI tool's adversarial review of this sub-slice, captured at `/workflow-close-slice`. Format: reviewer tool name + date + findings + how each was addressed (or why accepted as-is). Empty until the review runs. -->

## Deferred risks

<!-- FILL IN: risks, loose ends, or follow-up work consciously deferred from this sub-slice. Each entry: what, why deferred, who/when picks it up. Link to the parent slice plan if a follow-up sub-slice is scheduled. Empty is valid if nothing was deferred. -->
