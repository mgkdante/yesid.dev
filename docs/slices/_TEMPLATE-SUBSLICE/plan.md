# Sub-Slice NN<letter> — Implementation Plan

**Level 2 implementation plan.** Task-by-task, each with STOP gate. Spec lives in `spec.md`.

**Goal:** [one sentence from spec]
**Architecture:** [high-level approach]
**Tech Stack:** [relevant subset for this sub-slice]
**Multi-session:** [number of sessions estimated]
**Design spec:** `./spec.md`

## File Structure

### Files to create
```
path/to/new-file.ts        — [purpose]
```

### Files to modify
```
path/to/existing-file.ts   — [what changes]
```

### Files to delete
```
path/to/obsolete-file.ts   — [why]
```

---

## Task 1: <Name>

**Files:**
- Create/Modify: `path/to/file`
- Test: `path/to/test`

### Steps

- [ ] **Step 1:** [exact instruction]
- [ ] **Step 2:** [exact instruction]
- [ ] **Step 3:** Run `bun run test` + `bun run check`
- [ ] **Step 4:** Pre-flight visual check (UI tasks only)
- [ ] **Step 5:** Append entry to `log.md` + per-task section to `handoff.md`

**STOP. Ask Yesid to verify on localhost before moving to Task 2.**

---

## Task 2: <Name>

[Same structure as Task 1. Every task ends with STOP.]

---

## Execution Order

[Which tasks depend on which. Which can parallelize (Yesid approval required for parallel implementation).]

## Out of Scope

[Prevents drift.]

## Common pitfalls

[Traps to avoid in this sub-slice. Reference cloud/claude-knowledge/os-quirks/ for platform-specific gotchas.]
