// Placeholder — Zod schemas land here in 18c Phase 3 (services domain) and
// 18e/18f (projects, blog) after adapter-boundary parsePort alignment (F3).
// When the first schema lands, re-enable the `export * from './schemas'` line
// in packages/shared/src/index.ts.

export {};
