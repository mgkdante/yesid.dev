# yesid.dev Codex project config.
#
# workflow-overlord 4.x registers its shell-neutral hook runtime directly from
# the installed plugin. Do not add repo-local hook mirrors or duplicate hook
# registrations here. Keep only project feature flags.

[features]
hooks = true
remote_connections = true
suppress_unstable_features_warning = true
