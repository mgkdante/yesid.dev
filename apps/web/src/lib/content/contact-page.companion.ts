// Hand-written companion to the CMS-derived `contact-page.ts` (slice-18m).
//
// Currently holds nothing — `web3formsKey` lives on the generated module as
// a CMS column (block_contact_content.web3forms_key). If a future need arises
// for hand-written chrome on this page, add it here.
//
// Companion file kept (empty exports) so the barrel `export * from './contact-page.companion'`
// statement resolves cleanly without churn.

export {};
