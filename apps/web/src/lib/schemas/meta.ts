// SiteMetaSchema and friends moved to @repo/shared/schemas (site-hardening-a-plus).
// Re-exported here so existing $lib/schemas/meta importers keep resolving.
export {
	SiteLinksSchema,
	SiteAddressSchema,
	SiteOwnerSchema,
	SiteMetaSchema,
} from '@repo/shared/schemas';
