// Blog schemas moved to @repo/shared/schemas (site-hardening-a-plus).
// Re-exported here so existing $lib/schemas/blog importers keep resolving.
export { BlogCategorySchema, BlogAnimationSchema, BlogPostSchema } from '@repo/shared/schemas';
