// Service schemas moved to @repo/shared/schemas (site-hardening-a-plus).
// Re-exported here so existing $lib/schemas/service importers keep resolving.
export { ServiceSectionSchema, ServiceSchema } from '@repo/shared/schemas';
