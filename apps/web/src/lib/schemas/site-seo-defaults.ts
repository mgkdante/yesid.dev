// SiteSeoDefaultsSchema moved to @repo/shared/schemas (site-hardening-a-plus;
// it lives in the shared site-meta.ts, same singleton row as SiteMetaSchema).
export { SiteSeoDefaultsSchema } from '@repo/shared/schemas';
