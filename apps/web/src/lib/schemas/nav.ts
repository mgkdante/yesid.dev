// Nav schemas moved to @repo/shared/schemas (site-hardening-a-plus); the
// NavLink/MenuItem/ErrorPageContent interfaces moved to @repo/shared types in
// the same pass (re-exported from $lib/navigation/types).
export { NavLinkSchema, MenuItemSchema, ErrorPageContentSchema } from '@repo/shared/schemas';
